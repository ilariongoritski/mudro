# Reporter service
REPORT_BOT_TOKEN=
# required: reporter fails fast without explicit REPORT_CHAT_ID
REPORT_CHAT_ID=
REPORT_INTERVAL_MIN=30

# reporter may call API URLs in future
API_BASE_URL=http://api:8080
