# Review — 2026-04-11 (scheduled `rewie`)

Агент: Мудроборос. Режим: read-only review, без правок кода.
Ветка на момент снимка: `agent/20260405-project-review-pass` (3 незакоммиченных файла, мелкие правки).

## TL;DR
Основной риск — **selftest не покрывает services/\***. Вы коммитите изменения в casino/feed-api/auth-api под правилом "selftest перед коммитом", но реально прогоняются только два импортера. CI это вытягивает через `go test ./...`, но сам цикл агента работает вслепую. Это приоритет №1.

Остальное — гигиена: CI не гоняет frontend-тесты, BIBLE.md не существует (есть только draft), в рабочем дереве 27MB сталых бинарей + 259MB в `tmp/`.

---

## Находки

### 1. selftest уже, чем декларируется — HIGH
`Makefile:265`
```
selftest:
	$(GO) test ./tools/importers/vkimport/app ./tools/importers/tgimport/app
```
В `services/*` лежит 13 тест-файлов (casino engine/fairness/handlers, feed-api server+integration, auth-api, bff-web, api-gateway, movie-catalog, orchestration-api). Ни один не запускается.

Mission §4 требует `make selftest` зелёный перед PR. По факту зелёный selftest **ничего не говорит** об изменениях в casino/feed-api/auth-api — а именно там идёт вся активность последних коммитов (`fix(casino): harden fairness`, `feat(casino): integrated instant roulette`, `f9f1f3e perf: production optimizations`).

**Фикс:** расширить selftest до `go test ./services/... ./tools/importers/vkimport/app ./tools/importers/tgimport/app`. Если какие-то пакеты требуют БД и в sandbox не проходят — вынести их за флаг `-short` и гонять в CI.

**Риск отката:** нулевой, только правка Makefile.

### 2. CI не запускает frontend тесты — MEDIUM
`.github/workflows/ci.yml` — 3 джобы: `security` (gitleaks), `backend` (lint + `go test ./...`), `frontend` (lint + build).
`frontend/package.json` содержит `"test": "vitest run"`, но в CI его нет.

**Фикс:** добавить шаг `npm run test` в frontend-джоб (после lint, до build). Разрешено по Mission §3 (добавление проверок в CI).

**Примечание:** в памяти агента записано "4 джобы CI: test-backend, test-frontend, smoke-e2e, validate-microservices" — это неверно, обновить memory после ревью.

### 3. BIBLE.md не существует — MEDIUM (требует owner approval)
Mission §5 определяет `BIBLE.md` как канон (read-only) и `BIBLE.proposed.md` как черновик. В репозитории есть только `BIBLE.proposed.md` (594 байта). Без baseline-файла daily-отчёт про diff BIBLE.md↔BIBLE.proposed.md не имеет смысла.

**Нужно от владельца:** approve первичного `BIBLE.md` (можно взять текущий `BIBLE.proposed.md` как есть). Это решение политики — см. Mission §9.

### 4. Стале артефакты в рабочем дереве — LOW (hygiene)
```
mudro.exe           18.7 MB  (Mar 25)
casino.test.exe      8.6 MB  (Apr 4)
tmp/                  259 MB
out/                  1.7 MB
output/               856 KB
```
Всё корректно gitignored (tracked binaries: 0), но занимает место и путает редакторы. Предложение: добавить `make clean` с удалением `*.exe`, `tmp/`, `out/`, `output/` в корне.

**Риск отката:** нулевой.

### 5. Легаси `cmd/*` параллельно `services/*` — LOW (ongoing)
В `cmd/` 17 утилит (importer, tgimport, vkimport, memento, backfill…). В `services/` уже 9 сервисов включая новый `movie-catalog`. Миграция заявлена в работе — не блокер, просто напоминание не плодить новое в `cmd/`.

### 6. Незакоммиченные правки на текущей ветке — LOW
```
M frontend/src/features/casino/ui/RouletteWheelSVG.tsx  (-2, +1)
M services/casino/internal/casino/roulette.go          (-1, +1)
M services/casino/internal/casino/store.go             (-1, +1)
```
Итого 3/5 строк. Мелочь, но висит с 5 апреля. Либо коммитить в `agent/20260405-project-review-pass`, либо сбрасывать.

### 7. CRLF/LF шум в `git diff` — INFO
git ругается на line-endings в ~25 файлах. `.gitattributes` есть, но нормализация не прогнана. Один раз `git add --renormalize .` закроет.

### 8. Открытые agent/* ветки — INFO
```
agent/20260401-logger
agent/20260403-pre-mudro-mvp
agent/20260404-casino-stack
agent/20260405-project-review-pass  (current)
```
Плюс на origin есть `agent/random-circle-feature`. По Mission §2.1 удалять ветки без owner approval нельзя — просто отмечаю, что их 5 штук, накапливаются.

---

## Секреты и безопасность
- `.env` присутствует локально, **не трекается** (`.gitignore: .env`). OK.
- В истории явных утечек не видно, Gitleaks-джоба в CI активна.
- Секретов в коммитах/диффе не обнаружено.
- SSH/UFW/systemd — не трогали, за рамками ревью.

## Наблюдаемость
Append-only audit лог в `docs/audit/` существует, последние записи 2026-02-25, 2026-03-23. Этот отчёт туда же.

---

## Приоритизированные рекомендации (для будущих PR)

| # | Что | Риск | Оценка | Автономно? |
|---|-----|------|--------|------------|
| 1 | Расширить `selftest` до `services/...` | низкий | 1 файл (Makefile) | да |
| 2 | Добавить `npm run test` в CI frontend | низкий | 1 файл (ci.yml) | да |
| 3 | `make clean` для артефактов | нулевой | 1 файл | да |
| 4 | `git add --renormalize .` | нулевой | ~25 файлов lineendings | да, отдельный PR |
| 5 | Инициализация `BIBLE.md` из `.proposed` | политика | 1 файл | **нет — owner approval** |
| 6 | Обновить memory агента (CI=3 jobs, не 4) | - | - | вне репо |

Пункты 1–4 укладываются в один цикл ≤25 файлов/800 строк. 5 — блокер, нужно решение владельца.

## Что владельцу решить
- Approve пункта 5 (BIBLE.md bootstrap) — см. Mission §9 "изменение политики".
- Всё остальное можно катить по обычному циклу agent/yyyymmdd-slug → selftest → PR.
