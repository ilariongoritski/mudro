# Review — 2026-04-16 (scheduled `rewie`)

Агент: Мудроборос. Режим: автономный read-only review (sandbox git writes запрещены — Windows mount).
Ветка: `agent/20260405-project-review-pass` (HEAD `72bc45f`). Main: `origin/main` отстаёт на 12 коммитов.
Новых коммитов с 2026-04-13: **0**. Разрыв цикла — 3 дня без PR/push.

---

## TL;DR

- **WIP не закоммичен 3-й день подряд**: 25 M + 15 `??` файлов, ~400 строк diff на ветке `agent/20260405-*`. Владелец не мёржил и не делал push.
- **Хорошая новость:** бóльшая часть открытых пунктов из 20260412/13 фактически **исправлена в working tree**, но изменения висят без коммита:
  - CI переписан (5 jobs: security, backend-lint, backend-build, backend-unit, backend-integration, frontend) — *C1 resolved-in-tree*.
  - `go test -s` → `go test -short` — синтаксис корректен.
  - `selftest` → `test-unit` с `TEST_UNIT_PACKAGES` включающим `./services/...` — *contra audit 20260412*.
  - `services/casino/migrations/010_unified_fairness.down.sql` создан — *миграция reversible, новая находка из 20260413 закрыта*.
  - `BIBLE.md` создан (канон), `BIBLE.proposed.md` пересинхронизирован — *M1 resolved-in-tree*.
- **Остаётся открытым:** `make clean` (M2) всё ещё отсутствует. Smoke-test для `services/bot` (C2) — не видно новых файлов тестов в services/bot.
- **Главный риск:** всё это добро утеряется, если ветка будет сброшена. **Нужен коммит+PR.**

---

## §1. Статус пунктов из 20260413

| # | Пункт | 20260413 | 20260416 | Комментарий |
|---|-------|----------|----------|-------------|
| C1 | CI (security+backend+frontend) | ❌ | 🟡 WIP | В working tree всё есть, но не закоммичено |
| C2 | Smoke-test `services/bot` | ❌ | ❌ | Новых `*_test.go` в `services/bot/...` не вижу |
| M1 | `BIBLE.proposed.md` пересинхронизация | ❌ | 🟡 WIP | BIBLE.md + BIBLE.proposed.md оба валидны, но uncommitted |
| M2 | `make clean` | ❌ | ❌ | Цели нет |
| N1 | `010_unified_fairness.down.sql` | ❌ | 🟡 WIP | Создан, не закоммичен |
| N2 | Уборка stale remote веток (agent/*, claude/*, codex/*) | ❌ | ❌ | Не проверялось, remote не затрагивался |

---

## §2. WIP-инвентарь (uncommitted)

25 modified + 15 untracked:

- **Infra/CI:** `ci.yml` (+48/-6), `azure-container-webapp.yml`, `docker-compose.yml`, `docker-compose.prod.yml` (−20), `Makefile` (+25/-11).
- **Casino backend:** `services/casino/internal/casino/{roulette,store}.go` (тривиальные правки 1/1), +5 новых `*_test.go` (blackjack, bonus_telegram, plinko, roulette, store_integration) — **покрытие растёт, надо зафиксировать**.
- **Frontend:** 13 файлов, доминирует упрощение casino-miniapp-page (−67, −78) и sessionSlice (+26/-12). Добавлен `frontend/src/app/providers/AuthProvider.tsx` (untracked).
- **Docs:** `BIBLE.md` (new), 2 новых ADR (`0001-release-auth-path`, `0002-release-runtime-entrypoint`), 3 audit-файла, `PROJECT_REVIEW_2026-04-11.md`, 2 smoke/release чек-листа, `scripts/release-demo.sh`.

Цикл по лимитам: **25 файлов (на границе) / ~400 строк diff (в лимите)**. Один PR прошёл бы. Но три дня простоя.

---

## §3. Риски

1. **Потеря работы.** Крупный uncommitted diff + отсутствие push = если владелец случайно сбросит ветку или сломается .git/index на Windows — работа потеряется. *Mitigation:* собрать PR сегодня.
2. **Дрейф main.** Main отстаёт на 12 коммитов от ветки обзора. Фичи живут в long-lived ветке → растёт риск merge-боли. *Mitigation:* разбить на 2-3 тематических PR (CI+Make, casino tests, docs/BIBLE).
3. **/health vs selftest.** Не проверял — но если `selftest` реально включает `./services/...` с интеграционкой casino, нужно убедиться что он не требует PG/Redis локально. В Makefile `TEST_UNIT_PACKAGES` включает `./services/...` целиком, а среди новых тестов есть `store_integration_test.go` — он **поднимется в `test-unit`?** Потенциальное замедление/ломка selftest. *Проверить перед коммитом:* `go test -short ./services/casino/internal/casino/...` без PG.
4. **`-short` guard.** Новые `*_integration_test.go` должны уважать `testing.Short()` и skip'ать без БД. Иначе CI `backend-unit` сломается.

---

## §4. Рекомендация (без git-writes из sandbox)

Владельцу: сегодня закоммитить WIP. Предлагаемая разбивка:

1. **PR #1 — CI + Makefile + migration down:** `ci.yml`, `azure-container-webapp.yml`, `Makefile`, `docker-compose*.yml`, `services/casino/migrations/010_unified_fairness.down.sql`. (~7 файлов, быстрый review.)
2. **PR #2 — Casino tests + минорные правки backend:** 5 новых `*_test.go` в `services/casino`, `roulette.go`, `store.go`, `internal/chat/hub_test.go`. Проверить `testing.Short()` в `store_integration_test.go`.
3. **PR #3 — Frontend упрощение casino-miniapp + AuthProvider:** 13 frontend файлов. Риск UX-регрессии — прогнать через `npm run test` + `npm run build`.
4. **PR #4 — Docs/BIBLE:** `BIBLE.md`, `BIBLE.proposed.md`, ADR 0001/0002, audit'ы, чек-листы, `CHANGELOG.md`, `README.md` (пересобрано, −140 строк — **review осознанно**).

Перед любым коммитом на Windows-mount: делать через клавбота на RU17840 (из sandbox .git/index ломается — см. feedback memory).

---

## §5. Open items на завтра

- [ ] `make clean` цель.
- [ ] Smoke-test `services/bot`.
- [ ] Чистка stale remote веток (владелец).
- [ ] Проверить skip-guard в `store_integration_test.go`.
- [ ] Сравнить новый `README.md` с прежним — осознано ли удаление 140 строк?

---

Подпись: Мудроборос, scheduled=`rewie`, 2026-04-16.
