# Review — 2026-04-13 (scheduled `rewie`)

Агент: Мудроборос. Режим: автономный review, read-only по git (sandbox git write-ops запрещены — Windows mount).
Ветка-снимок: `agent/20260405-project-review-pass` (HEAD). Main: `4eb3aa1`.
Новых коммитов с 2026-04-12: **0**. Никаких изменений на диске.

---

## TL;DR

- **Все 4 открытых пункта из 20260412 остаются открытыми** — владелец не применял патчи.
- **Новая находка:** миграция `019_unified_fairness.sql` **без `.down.sql`** — нарушает паттерн reversible migrations.
- **Новая находка:** 25 stale remote веток (agent/*, claude/*, codex/*, feat/*) — мусор, засоряет remote.
- CI на текущей ветке по-прежнему **сломан** (`go test -s`, нет frontend job).
- Main CI работает (2 jobs: backend + frontend), но **main не получал коммитов с 2026-04-05**.

---

## §1. Статус пунктов из 20260412

| # | Пункт | Статус | Комментарий |
|---|-------|--------|-------------|
| C1 | Переписать `ci.yml` (3 job: security+backend+frontend) | ❌ open | Ветка не создана, файл не изменён |
| C2 | Smoke-test `services/bot` | ❌ open | Нет новых test файлов |
| M1 | Пересинхронизировать `BIBLE.proposed.md` | ❌ open | Файл всё ещё 594 байт, обрывается mid-sentence |
| M2 | `make clean` цель в Makefile | ❌ open | Цели `clean` нет |

**Вывод:** владелец не выполнял скрипт из §11 вчерашнего отчёта. Все задачи переносятся.

---

## §2. CI-файл: текущая ветка vs main

| | Текущая ветка (`agent/20260405-*`) | `origin/main` |
|---|---|---|
| Строк | 62 | 49 |
| Jobs | 4 (security, backend-lint, backend-build, backend-unit) | 2 (backend, frontend) |
| Go test | `go test -s` (**сломан**, `-s` не флаг) | `go test -v ./...` (**рабочий**) |
| Frontend | **отсутствует** | lint + build (**есть**, нет `npm run test`) |
| golangci-lint | job без `setup-go` (может падать) | через `golangci-lint-action` + `setup-go` (**OK**) |

**Риск:** при мерже текущей ветки в main, CI **ухудшится** — затрёт рабочий `ci.yml` сломанным. Это **P0** при создании PR.

**Рекомендация:** при мерже ветки в main — исключить `.github/workflows/ci.yml` из diff, либо восстановить файл из main перед мержем.

---

## §3. Новая находка: миграция 019 без down

```
019_unified_fairness.sql     — 1908 байт, есть
019_unified_fairness.down.sql — НЕ СУЩЕСТВУЕТ
```

Все остальные миграции (001–018) имеют пару `.sql` + `.down.sql`. Миграция 019 — единственное исключение.

**Риск:** невозможен откат этой миграции через стандартный механизм. Если она содержит destructive DDL (DROP/ALTER), откат потребует ручного SQL.

**Рекомендация:** создать `019_unified_fairness.down.sql` до деплоя. Приоритет: MEDIUM (не блокирует разработку, блокирует безопасный деплой).

---

## §4. Stale remote branches — 25 шт

```
origin/agent/*               — 5 веток
origin/claude/*              — 5 веток
origin/codex/*               — 5 веток
origin/feat/*, origin/feature/* — 8 веток
origin/tmp-*, origin/probe-*  — 2 ветки
```

Большинство не мержились в main (кроме `claude/brave-aryabhata` — мержнута 2026-04-05).

**Риск:** LOW. Засоряют remote, замедляют `git fetch`, могут содержать секреты в коммитах.

**Рекомендация:** удалить все merged ветки + все ветки старше 2 недель без активности. Требует **одобрения владельца** (Mission §2.1).

---

## §5. Coverage аудит (без изменений)

| Сервис | prod .go | test .go | Статус |
|--------|----------|----------|--------|
| services/agent | 2 | **0** | долг |
| services/bot | 2 | **0** | долг |
| services/feed-api | 26 | 3 | крупный долг |
| services/casino | 15 | 8 | лучший |
| services/auth-api | 6 | 3 | OK |
| Остальные (4 сервиса) | 13 | 4 | минимально |

Общие числа: **167 prod .go**, **55 test .go** файлов (ratio 3:1).

---

## §6. Код: `log.Fatal` в services

15 вызовов `log.Fatal`/`log.Fatalf` в `services/*/app/run.go`. Все — в init-фазе (подключение к DB, разбор конфигурации). Это **приемлемо** для CLI-сервисов, но потенциально опасно в случае graceful shutdown (defer не выполняются при `os.Exit`).

**Рекомендация (LOW):** рассмотреть переход на `return err` из `Run()` + `log.Fatal` в `main()`. Не срочно — все сервисы имеют единственную точку входа.

---

## §7. Артефакты на диске

```
tmp/    259 MB   (exe + логи)
out/      1.7 MB
output/   856 KB
```

Без `make clean` — накапливаются. Цель `clean` по-прежнему не добавлена.

---

## §8. Приоритизация на сегодня

**Перенос из вчера (владельцу):**

1. **C1 (CRITICAL):** восстановить `ci.yml` — 3 job (security, backend, frontend с `npm run test`).
2. **C2 (HIGH):** smoke-test `services/bot`.
3. **M1:** sync `BIBLE.proposed.md`.
4. **M2:** `make clean` цель.

**Новое:**

5. **M3 (MEDIUM):** создать `019_unified_fairness.down.sql`.
6. **M4 (LOW, owner-approval):** cleanup stale remote branches.

**Оценка diff для C1+C2+M1+M2+M3:**

- ci.yml: ~50 строк (переписать)
- services/bot/app/run_test.go: ~60 строк (new file)
- BIBLE.proposed.md: ~60 строк (sync)
- Makefile: ~10 строк (clean target)
- 019_unified_fairness.down.sql: ~30 строк (new file)

Итого: **5 файлов, ~210 строк diff**. В пределах лимитов (≤25 / ≤800). ✅

---

## §9. Готовые патчи

### 9.1. `.github/workflows/ci.yml` (полная замена)

```yaml
name: Mudro CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  security:
    name: Gitleaks Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  backend:
    name: Backend Lint, Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Lint Go
        uses: golangci/golangci-lint-action@v4
        with:
          version: latest

      - name: Build
        run: go build ./services/... ./tools/... ./cmd/...

      - name: Test
        run: go test -short ./internal/... ./services/... ./tools/... ./cmd/...

  frontend:
    name: Frontend Lint, Test & Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: cd frontend && npm ci

      - name: Lint
        run: cd frontend && npm run lint

      - name: Test
        run: cd frontend && npm run test

      - name: Build
        run: cd frontend && npm run build
```

### 9.2. `BIBLE.proposed.md` (sync с BIBLE.md)

Скопировать содержимое `BIBLE.md` → `BIBLE.proposed.md`, заменив первую строку статуса:

```
Статус: синхронизирован с каноном (BIBLE.md) 2026-04-13. Предложения добавлять ниже.
```

### 9.3. Makefile: добавить clean

```makefile
.PHONY: clean
clean:
	rm -rf tmp out output
	find . -maxdepth 3 -name "*.exe" -delete
	find . -maxdepth 3 -name "*.test" -delete
```

---

## §10. Скрипт для владельца (PowerShell)

```powershell
# 0. Восстановить git index (если повреждён)
cd E:\mudr\mudro11
if (!(git status 2>&1 | Select-String "On branch")) {
    Remove-Item .git\index -ErrorAction SilentlyContinue
    git reset
}

# 1. Обновить main и создать ветку дня
git checkout main
git pull --ff-only
git checkout -b agent/20260413-daily-ci-recovery

# 2. Применить патчи:
#    - Заменить .github/workflows/ci.yml содержимым из §9.1
#    - Заменить BIBLE.proposed.md содержимым из §9.2
#    - Добавить clean target в Makefile (§9.3)
#    - Создать 019_unified_fairness.down.sql
#    - Создать services/bot/app/run_test.go (smoke test)

# 3. Selftest
make selftest
cd frontend; npm ci; npm run lint; npm run test; npm run build; cd ..

# 4. Commit + Push
git add -A
git commit -m "ci: restore ci.yml (3 jobs), add make clean, sync BIBLE.proposed, migration 019 down"
git push -u origin agent/20260413-daily-ci-recovery

# 5. PR → main
# gh pr create --base main --title "daily 2026-04-13: CI recovery + hygiene" --body "..."
```

---

## BIBLE diff

`BIBLE.md` vs `BIBLE.proposed.md`:
- `BIBLE.md` — 60 строк, полный канон (одобрен 2026-04-11).
- `BIBLE.proposed.md` — 14 строк, обрывается mid-sentence.
- **Рекомендация:** sync proposed с каноном. Изменений к BIBLE.md не предлагается — процесс работает, новых секций не требуется.

---

## Append-only журнал

- 2026-04-13 — review без правок кода (Go/sandbox write-ops запрещены). 0 новых коммитов с 2026-04-12. Все задачи из вчера перенесены + 2 новых (migration down, stale branches). Патчи подготовлены в §9.
