# Daily Review — 2026-04-20 (Мудроборос)

## Среда выполнения
- Sandbox: нет Go-компилятора, нет write-доступа к mounted repo.
- Обзор: read-only статический анализ структуры, кода, git-состояния.

---

## 1. Состояние репозитория

**Текущая ветка**: `codex/stabilize-main-runtime`
**Отставание от main**: 10+ коммитов (в основном casino-стабилизация)
**Diff vs main**: 161 файлов, +11777 / -1855 строк — **далеко за пределами** лимита 25 файлов / 800 строк.

**Проблема**: ветка `codex/stabilize-main-runtime` содержит огромный diff. Это нарушает BIBLE §цикл (≤25 файлов / ≤800 строк). Рекомендация: не мержить эту ветку целиком. Разбить на атомарные PR или cherry-pick нужное.

**Stale worktrees**: 12 worktrees в `.claude/worktrees/`, суммарно 581MB. Все gitignored, но занимают место на диске.

**npm-cache**: 95MB в `frontend/.npm-cache/` — тоже gitignored, но мусор.

---

## 2. Безопасность

- `.gitignore` корректно исключает `env/*`, `.skaro/secrets.yaml`, `.env`.
- Только `*.env.example` файлы попадают в git — проверено.
- `secrets.yaml` пуст (`api_keys: {}`).
- Конфиг читает секреты из env-переменных (`os.Getenv`) — нормально.
- Нет хардкоженных токенов/паролей в `.go` файлах (grep по `hardcoded` — чисто).
- **Нет проблем** по секретам.

---

## 3. Feed MVP — готовность

### Backend (feed-api)
- `handleFront` — работает: пагинация, source-фильтр, сортировка, поиск (q), cursor-based pagination.
- `handlePosts` — полноценный endpoint с cursor + page.
- Метаданные: `total_posts`, `last_sync_at`, `source_stats` — всё подключено.
- Likes, comments — endpoints зарегистрированы.
- Casino proxy живёт в том же сервисе (feed-api) — это архитектурный долг, но для MVP допустимо.

### Frontend (React/TS)
- `FeedPage` → `FeedWidget` — полная реализация:
  - RTK Query (`useGetFrontQuery`, `useLazyGetPostsQuery`).
  - Cursor-based infinite scroll ("Показать ещё").
  - Skeleton loading, error state, empty state — всё есть.
  - `PostCard`, `PostDetailDrawer` — карточки и drawer.
  - `FeedControls` — фильтры source/sort/query.
- FSD-структура соблюдена: entities/post, features/feed-controls, widgets/feed, pages/feed-page.
- Роутинг: `/` → FeedPage (главная), `/casino`, `/chat`, `/movies` и др.

### Оценка
**Feed MVP функционально готов** — backend API и frontend UI покрывают базовый сценарий "просмотр ленты VK+TG с фильтрами и пагинацией". Данные уже в БД (подтверждено ранее).

### Что осталось до публичной ленты
1. **Деплой проверка** — убедиться что feed-api на сервере поднимается и `/api/front` отдаёт данные.
2. **Frontend build** — `npm run build` должен проходить без ошибок.
3. **CORS / proxy** — проверить что фронт корректно обращается к API (не localhost).
4. **Тесты** — `server_integration_test.go` есть, но нужен прогон.

---

## 4. CI/CD

CI (`ci.yml`) — 6 jobs:
- security (gitleaks)
- backend-lint, backend-build, backend-unit, backend-integration
- frontend (build + lint + test)

CI покрывает основные проверки. **Нет проблем** в конфигурации.

---

## 5. Технический долг

| Приоритет | Проблема | Рекомендация |
|-----------|----------|--------------|
| **P0** | Ветка `codex/stabilize-main-runtime` с 161 файлами diff — не мержить целиком | Cherry-pick нужные фиксы, остальное — в отдельные PR |
| **P1** | 12 stale worktrees (581MB) | Очистить: `git worktree prune` + удалить директории |
| **P1** | Casino proxy в feed-api (server.go 77-101) | Выделить в отдельный service позже, для MVP — ок |
| **P2** | `buildPostsVisibilityWhere` строит SQL вручную | Рефакторить в query builder или хотя бы вынести в repository |
| **P2** | `handleFeed` использует `s.countVisiblePosts` (старый метод с pointer), а `handleFront` — `s.postsSvc.CountVisiblePosts` (новый) | Унифицировать |
| **P3** | `frontend/.npm-cache/` (95MB) | Удалить, уже gitignored |

---

## 6. BIBLE diff

`BIBLE.md` vs `BIBLE.proposed.md` — **практически идентичны**.

Единственные различия:
1. `BIBLE.md` содержит шапку со статусом и историей approve.
2. Минорные различия в формулировках ("`Селftest`" vs "`make selftest`" в proposed — опечатка "Селftest").

**Предложение**: исправить опечатку "Селftest" → "`make selftest`" в `BIBLE.proposed.md`. Других изменений для approve нет.

---

## 7. Рекомендуемые действия (следующий цикл)

1. **[Критичный]** Разобраться с веткой `codex/stabilize-main-runtime` — определить какие коммиты нужны в main, сделать atomic PR.
2. **[Критичный]** Прогнать `make selftest` на сервере для текущего main — убедиться что build и unit тесты зелёные.
3. **[Мелочь]** Очистить stale worktrees.
4. ~~**[Мелочь]** Исправить опечатку в BIBLE.proposed.md.~~ **DONE** (2026-04-20, pass 2).

---

## 8. Pass 2 — глубокий анализ (2026-04-20, Мудроборос, автоматический прогон)

### 8.1 BIBLE.proposed.md — синхронизация
Исправлено 3 расхождения с BIBLE.md:
- "Селftest" → "`make selftest`" (опечатка).
- Убран лишний пример `(как 2026-04-11-review.md)` из п.2.
- "Ограничения остаются:" → "Ограничения:" (соответствие канону).

**Результат**: BIBLE.proposed.md теперь полностью совпадает по содержанию с BIBLE.md (кроме шапки статуса/истории approve). Нет pending изменений для approve владельца.

### 8.2 Анализ кода — дополнительные находки

**Тестовое покрытие (по файлам)**:
- `services/` — 18 test-файлов. Покрыты: api-gateway, auth-api, bff-web, casino (6 файлов), feed-api (3 файла), movie-catalog, orchestration-api.
- `internal/` — 27 test-файлов. Хорошо покрыт bot (16 файлов), auth, catalog, chat, config, events, media, ratelimit, reporter.
- **Без тестов**: `services/agent/`, `services/bot/` (bot-сервис, не internal/bot), `internal/importer/`, `internal/tgexport/`, `internal/commentmodel/`.
- **Долг**: services/agent и services/bot не имеют ни одного теста.

**Потенциальные проблемы**:
- `cmd/backfill/main.go` использует `log.Fatalf` (5 мест) — для CLI-утилит допустимо, но при рефакторинге стоит заменить на graceful exit.
- `cmd/mudro/main.go` использует `fmt.Println` для usage — ок для CLI.
- Нет `panic()` вызовов в production-коде (вне тестов) — хорошо.
- Casino proxy в feed-api: 289 строк boilerplate (`casino_proxy.go`). 20+ handler-функций, каждая — обёртка над `proxyCasino`. Архитектурный долг, но не блокер для MVP.

**CI наблюдение**:
- `backend-integration` запускает только `./services/feed-api/internal/feed` — другие сервисы с `_integration_test.go` (casino `store_integration_test.go`) не прогоняются в CI.
- Рекомендация: расширить integration job или добавить отдельный casino integration job.

### 8.3 Feed MVP — блокеры для публичной ленты

По результатам анализа кода, фронтенд и бэкенд ленты функционально готовы. Ключевые блокеры — операционные:
1. Деплой feed-api на сервере + проверка `/api/front`.
2. Frontend build (`tsc -b && vite build`) — требует прогона.
3. Vercel/proxy конфигурация — `vercel.json` есть, нужно проверить rewrites.

### 8.4 Зависимости
- Go 1.24, Node 20, React 19, Vite 7, Vitest 4 — всё свежее. Нет устаревших major-версий.
- `@supabase/supabase-js` в зависимостях — не используется активно (возможно остаток). Проверить при cleanup.
