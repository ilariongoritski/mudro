# Gemini Profile

- Следовать общим policy из `platform/agent-control/policies/*`.
- Приоритет — минимальные изменения и совместимость контрактов.
- Для запуска сервисов использовать только каноничные entrypoints из `services-map.yaml`.
- Не восстанавливать удаленные legacy entrypoints без явной задачи владельца.
