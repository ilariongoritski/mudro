# TODO (оперативный)

Правила:
- Только конкретные задачи с измеримым результатом.
- Формат: дата, приоритет, зона, цель, следующий шаг.
- Закрытые задачи переносить в `.codex/done.md`.
- Крупные цели хранить в `.codex/todo_big.md`, здесь держать исполняемый short-list.

## Актуализация 2026-03-18
- [ ] 2026-03-18 | P0 | area:branch-sync | Свести в один канонический контур изменения Antigravity и текущей копии (`auth`, runtime guards, `tgrootmerge`, premium auth UI, Vercel/Nginx routing)
  - Контекст: внешние логи 18.03 описывают уже auth-enabled personal-feed MVP, а текущая копия содержит отдельный staged-срез runtime/feed изменений; между контурами есть расхождение по тестовому статусу
  - Следующий шаг: сравнить `mudro11` и `mudro11-main`/Antigravity, решить набор merge/cherry-pick, затем прогнать единый `go test ./...` + frontend build/lint
- [ ] 2026-03-18 | P1 | area:release-sync | После branch-sync закрыть общий MVP backlog из Antigravity: реальный контент на VPS, sync кода с Git, восстановление worker, HTTPS, mobile polish
  - Контекст: это общий срез из `backlog_tomorrow.md`; до sync веток нельзя считать эти пункты закрытыми или неактуальными
  - Следующий шаг: после сведения веток взять порядок `content+worker -> HTTPS -> mobile`

## P0 (сделать в первую очередь)
- [ ] 2026-02-25 | P0 | area:ops | Синхронизировать сервер с текущим Makefile (`migrate-agent` отсутствует)
  - Контекст: подтверждено, что `/root/projects/mudro` не является git-репозиторием (`fatal: not a git repository`), `find /root -name .git` ничего не нашел
  - Контекст (2026-03-05): VPS `91.218.113.247` переустановлен на Ubuntu 24.04, начата повторная инициализация
  - Следующий шаг: завершить bootstrap (`admin` + SSH key), затем пересоздать каталог как git-клон и сверить `Makefile`
- [ ] 2026-02-25 | P0 | area:db | Применить миграцию `002_agent_queue.sql` на сервере
  - Контекст: без таблицы `agent_queue` planner/worker нерабочие
  - Контекст (2026-03-05): SSH восстанавливается после переустановки VPS
  - Следующий шаг: после базовой настройки VPS выполнить `make migrate-agent && make agent-plan-once`
- [ ] 2026-02-25 | P0 | area:security | Ротация засвеченных секретов (Telegram/OpenAI)
  - Контекст: токены и ключи публиковались, риск компрометации высокий
  - Блокер (2026-02-28): ротация требует доступа к провайдерам/серверу (`BotFather`, OpenAI, `.env` на VPS)
  - Следующий шаг: перевыпустить токены/ключи и обновить `/root/projects/mudro/.env`, затем проверить запуск бота/API

## P1 (ближайшие 1-3 дня)
- [x] 2026-02-28 | P1 | area:ops | Восстановить доступ к Docker daemon для локального health loop
  - Контекст: доступ к Docker подтвержден при запуске вне sandbox; health loop пройден полностью
  - Следующий шаг: для Docker-команд из Codex запускать с разрешением на docker.sock
- [ ] 2026-02-27 | P1 | area:deploy | Поднять внешний URL ленты через Vercel
  - Контекст (2026-03-05): production deploy выполнен, `GET /healthz` = 200, но `GET /feed` = 500
  - Блокер (2026-03-05): в проекте Vercel отсутствуют production env (`DSN` не задан)
  - Следующий шаг: добавить внешний `DSN` в Vercel, выполнить redeploy и проверить `/feed?limit=2` на 200
- [x] 2026-02-28 | P1 | area:ops | Подготовить `docker-compose.prod.yml` для `api+db+agent+reporter`
  - Контекст: добавлен `docker-compose.prod.yml` с сервисами `db/api/agent/reporter` и healthcheck
  - Следующий шаг: при подключении VPS задать production env и поднять `docker compose -f docker-compose.prod.yml up -d`
- [x] 2026-02-28 | P1 | area:ops | Runbook восстановления после ребута
  - Контекст: создан `docs/ops-runbook.md` с recovery-порядком и типовыми сбоями
  - Следующий шаг: на следующем чистом старте проверить runbook по шагам
- [x] 2026-02-28 | P1 | area:agent | Добавить `review-gate` (approve/reject) для risky-задач
  - Контекст: добавлены статусы `waiting_approval/rejected`, миграция `004_agent_review_gate.sql`, команды `approve/reject` в `cmd/agent`
  - Следующий шаг: использовать `make agent-approve TASK_ID=<id>` и `make agent-reject TASK_ID=<id> REASON='<reason>'`
- [x] 2026-02-28 | P1 | area:agent | Реальные типы задач для worker (не только skeleton)
  - Контекст: в `internal/agent/worker.go` добавлен task kind `health_check` с запуском `make test`
  - Следующий шаг: расширить safe-пайплайн (например, `make dbcheck` + `make test`) без destructive-операций
- [ ] 2026-02-28 | P1 | area:security | Включить fail2ban и закрыть SSH парольный вход
  - Контекст (2026-03-05): новый VPS установлен, можно выполнять hardening сразу после проверки входа по ключу
  - Следующий шаг: настроить `sshd_config` (`PasswordAuthentication no`, `PermitRootLogin no`) и включить jail для sshd
- [ ] 2026-03-05 | P1 | area:llm | Поднять локальный LLM-контур на VPS (`Ollama + Open WebUI`) как backup для бота
  - Контекст: сервер `4 vCPU / 8 GB RAM`; реалистичный профиль для CPU-only — модели 3B (q4), 7B (q4) как опционально медленный режим
  - Следующий шаг: подготовить docker-compose для `ollama+open-webui`, включить auth и ограничить доступ через reverse proxy/firewall

## P2 (на неделю)
- [ ] 2026-02-27 | P2 | area:import | Повторно импортировать TG после догрузки медиафайлов
  - Контекст: сейчас загружен `result.json` без медиафайлов; текст и метаданные импортированы
  - Следующий шаг: докинуть недостающие файлы в `data/tg-export/` и повторить `go run ./cmd/tgimport ...` + `go run ./cmd/tgload ...` (upsert)
- [x] 2026-02-28 | P2 | area:observability | Метрики агента (`tasks_total/success/fail/retry`)
  - Контекст: добавлена таблица `agent_task_events`, агент пишет lifecycle-события задач, reporter читает 24ч-агрегаты
  - Следующий шаг: дополнительно вывести эти метрики в `/health` (bot) для единообразия каналов
- [x] 2026-02-28 | P2 | area:events | Внедрить Kafka publisher для событий `tasks`
  - Контекст: добавлен runtime publisher при `KAFKA_ENABLED=true`, события задач публикуются в `KAFKA_TOPIC_TASKS`
  - Следующий шаг: расширить event-pipeline на `posts/comments` и добавить consumer + DLQ
- [x] 2026-02-28 | P2 | area:reporter | Ежедневный digest по агенту в Telegram
  - Контекст: digest теперь включает метрики агента за 24ч (created/done/failed/retry, queue state, top kinds)
  - Следующий шаг: добавить алерт-порог (например, fail>0 или waiting_approval>0) и краткий action plan в конец отчета
- [ ] 2026-02-25 | P2 | area:repo | Убрать/перенести backup/save артефакты из рабочей структуры
  - Контекст: шум в репозитории ухудшает обзор и автопоиск задач
  - Следующий шаг: инвентаризировать артефакты и вынести в `docs/legacy` или удалить по согласованию
- [ ] 2026-02-25 | P2 | area:bot | Доработать `/feed5` и диагностировать причину нестабильности
  - Контекст: есть зафиксированная «проблема feed5» в памяти
  - Следующий шаг: добавить защиту от пустого API-ответа и тест-кейс
- [ ] 2026-02-25 | P2 | area:bot | Проверить читабельность `/health` на 3 реальных сценариях
  - Контекст: отчет должен быть полезен в 1 экран телефона
  - Следующий шаг: прогнать сценарии «все ок/частичный сбой/критический сбой»

## P3 (беклог)
- [ ] 2026-02-25 | P3 | area:frontend | Подготовить документ «мысли о фронтенде» (React+TS+RTK+RTK Query+Vite+FSD)
  - Контекст: нужно заранее синхронизировать backend-контракты и ожидания фронта
  - Следующий шаг: создать `.codex/notes/черновик мысли о фронтенде.md`
- [ ] 2026-02-25 | P3 | area:memory | Сделать обзорный файл «память/личность проекта» для быстрых онбординг-ответов
  - Контекст: запрос на единое представление контекста проекта уже есть
  - Следующий шаг: ввести `.codex/notes/project-identity.md` с регулярным обновлением

## Ссылка на стратегию
- Масштабные цели и дорожная карта: `.codex/todo_big.md`
- [ ] 2026-02-25 | P2 | area:bot | <после команды время до сих пор не показывает траты воды, ремя работы:
- Сегодня: 00:46:00 (46 мин, 2760 сек), прогонов: 7
- Всего: 05:28:00 (328 мин, 19680 сек), прогонов: 56
- Генерация ответов: 00:00:14 (0 мин, 14 сек), ответов: 59, среднее: 249 мс
- Память JSON: .codex/memory.json, вот сейчас, я хочу затраты точно формата «сегодня часов минут, всего часов минут, количество обращений, затраты воды суммарно за день и вообще»>
  - Контекст: добавлено из Telegram `/todoadd`
  - Следующий шаг: декомпозировать и выполнить
- [ ] 2026-02-25 | P2 | area:memory | Проверить и классифицировать новые файлы (18 шт.) в памяти проекта
  - Контекст: авто-добавлено `/memento`
  - Следующий шаг: обновить TOP10/TODO/DONE по новым изменениям
- [ ] 2026-02-25 | P2 | area:repo | Почистить или перенести backup/save артефакты из рабочей структуры
  - Контекст: авто-добавлено командой /find
  - Следующий шаг: уточнить план и выполнить
- [x] 2026-02-28 | P1 | area:import | Переимпортировать TG-комментарии из того же экспорта, что и TG-посты
  - Контекст: выполнен `go run ./cmd/tgcommentsimport -dir data/tg-export`, итог `imported_comments=55`
  - Следующий шаг: при обновлении экспорта повторять импорт из той же папки `data/tg-export`
- [ ] 2026-02-28 | P1 | area:deploy | Подключить публичное хранилище медиа (Vercel Blob/S3) и заменить локальные пути в `posts.media`
  - Контекст: backend уже готов определять media type и fallback, но для реального рендера нужны публичные URL
  - Блокер (2026-02-28): не выбрано/не подключено production storage и нет ключей доступа в текущей сессии
  - Следующий шаг: выбрать Blob/S3, загрузить файлы, выполнить backfill `url/preview_url` для TG-постов
- [ ] 2026-02-28 | P1 | area:media | Заменить placeholder media URL на реальные ссылки из Blob/S3
  - Контекст: временно используются `picsum/placehold`, чтобы лента выглядела прилично перед деплоем
  - Блокер (2026-02-28): без подключенного Blob/S3 невозможно выполнить финальный backfill URL
  - Следующий шаг: после выбора storage выполнить загрузку файлов и backfill URL в `posts.media`
- [x] 2026-02-28 | P1 | area:import | Завершить импорт VK-экспорта (`vk_wall_*.json`) в БД
  - Контекст: выполнен повторный импорт из `/home/gorit/vk-export`, итог `DONE: total posts=1087`
  - Следующий шаг: при следующем импорте делать проверку `select source,count(*) from posts group by source`
- [ ] 2026-02-28 | P2 | area:repo | Добавить unit-тесты на ключевые bot/api сценарии
  - Контекст: авто-добавлено командой /find
  - Следующий шаг: уточнить план и выполнить
- [ ] 2026-03-01 | P2 | area:memory | Проверить и классифицировать новые файлы (727 шт.) в памяти проекта
  - Контекст: авто-добавлено `/memento`
  - Следующий шаг: обновить TOP10/TODO/DONE по новым изменениям
- [ ] 2026-03-05 | P2 | area:memory | Проверить и классифицировать новые файлы (9 шт.) в памяти проекта
  - Контекст: авто-добавлено `/memento`
  - Следующий шаг: обновить TOP10/TODO/DONE по новым изменениям
- [ ] 2026-03-05 | P2 | area:memory | Проверить и классифицировать новые файлы (1 шт.) в памяти проекта
  - Контекст: авто-добавлено `/memento`
  - Следующий шаг: обновить TOP10/TODO/DONE по новым изменениям

## Snapshot focus (dialogs + registration)
- [ ] 2026-03-04 | P1 | area:snapshot | Открыть отдельную snapshot-ветку под диалоги+регистрацию после закрытия media-blockers
  - Контекст: критерии готовности зафиксированы в `.codex/memory_dialogs_registration_snapshot.md`
  - Следующий шаг: после закрытия задач `area:media` и `area:import` создать `snapshot/2026-03-dialogs-registration`
- [ ] 2026-03-04 | P1 | area:dialogs | Зафиксировать MVP-контракт диалогов (модели + API endpoints)
  - Контекст: фид и импорт уже готовы как data-base; нужен отдельный контракт для диалогового слоя
  - Следующий шаг: оформить ADR/RFC с сущностями `dialog/message/read_state` и пагинацией
- [ ] 2026-03-04 | P1 | area:auth | Подготовить MVP регистрации по email (flow + SMTP + TTL токены)
  - Контекст: в README уже есть направление по email-верификации, но нет детализированного плана реализации
  - Следующий шаг: добавить `env/auth.env.example` и описание auth-инвариантов в docs
- [ ] 2026-03-04 | P1 | area:ops | Прогнать `make worker-loop` на машине с установленным docker и доступом к proxy.golang.org
  - Контекст: в текущем окружении отсутствует docker binary и `go test` частично блокируется 403 при загрузке модулей
  - Следующий шаг: выполнить полный автономный цикл в нормальном локальном окружении и приложить лог `.codex/logs/<run>/index.md`
- 2026-03-05 | P1 | area:frontend | Завершить MVP фронтенда на React+TS+RTK+RTK Query+Vite+FSD
  - Контекст: базовый SPA уже реализован в `frontend/`, добавлена HTML-демо страница `docs/frontend-preview.html`
  - Следующий шаг: подключить e2e/интеграционные тесты и при необходимости SSR/static serving через backend
- [ ] 2026-03-05 | P1 | area:ops | Восстановить локальные инструменты Windows (go, make, psql) для полного health loop без обходов
  - Контекст: на шаге go test ./... получен CommandNotFoundException; БД-проверки выполняются только через docker compose exec
  - Следующий шаг: установить Go toolchain + GNU Make + psql client или утвердить контейнерный запуск тестов
- 2026-03-05 | P1 | area:media | Довести публичность media URL для демо-ленты (HTTP coverage)
  - Контекст: в единой БД `5433` media items=1778, из них `ok_http=1132`, `non_http_local=459`, `missing://=54`, `empty=133`; постов с media=1334, с публичным HTTP media=818
  - Следующий шаг: backfill/публикация локальных media (`video_files/*`, относительные пути) в публичное хранилище и обновление `posts.media`
- 2026-03-05 | P1 | area:frontend | Доработать рендер media в React-ленте для объектов без `is_image/is_video` флагов
  - Контекст: API возвращает raw media JSON, где для части записей есть `kind/url` без вычисленных boolean-флагов
  - Следующий шаг: добавить клиентскую нормализацию media-kind по `kind/url` (аналог логики backend `parseMediaItems`)
- 2026-03-05 | P1 | area:media | Закрыть остаток media без URL (187 вложений)
  - Контекст: после backfill для демо в `5433` осталось `empty_url=187` (в основном audio/file без публичного файла)
  - Следующий шаг: либо догрузить реальные файлы в публичное хранилище, либо явно маркировать их как metadata-only в API/фронте

- [ ] 2026-03-05 | P1 | area:ops | Закрепить серверный API как systemd service (автостарт + рестарт + env DSN) вместо фонового go run
- [ ] 2026-03-06 | P1 | area:openclaw | Выполнить подготовленные скрипты OpenClaw на VPS и завершить onboarding
  - Контекст: в репе готовы `scripts/openclaw/*.sh` и runbook `docs/openclaw/openclaw_server_runbook.md`
  - Следующий шаг: на сервере запустить bootstrap/install/checks и пройти OAuth+Telegram pairing
- [ ] 2026-03-06 | P0 | area:openclaw | Поднять рабочий gateway OpenClaw на VPS (/root/vpn-test) без 1006/connection refused
  - Контекст: onboarding complete, VPN active (US IP), но порт 18789 не слушает при текущем запуске
  - Следующий шаг: стабилизировать CLI path/binary, запустить gateway как сервис/daemon и подтвердить openclaw health + ответ Telegram-бота
- [ ] 2026-03-08 | P1 | area:openclaw | Прогнать smoke-test Telegram после tuning (`maxConcurrent=1`, `streaming=off`) и зафиксировать частоту `server_error` за 24ч
  - Контекст: 3 подряд `server_error` пришли от upstream провайдера; локальный gateway/VPN в норме
  - Следующий шаг: выполнить серию коротких/длинных задач и при необходимости добавить retry/backoff policy
- [ ] 2026-03-08 | P1 | area:openclaw | Добавить операционный мониторинг cold-start времени gateway (целевое: <120 сек) и alert при превышении, т.к. текущий старт может занимать ~10-12 минут.

- [tech] Для удалённых SSH-операций не использовать WSL-ключи из /mnt/c (ошибка прав 0777). Использовать нативный PowerShell ssh/scp или ключ в Linux FS с chmod 600. Для длинных команд: только script-file + upload, без inline-кавычек.
- 2026-03-13 | P1 | area:frontend-ui | Доделать visual UI MVP: отдельный landing/overview page, empty state, loading skeletons, detail drawer для поста
  - Контекст: hero + toolbar + post-card уже усилены через Magic-inspired референсы, но UI MVP еще не закрыт как связный продуктовый сценарий
  - Следующий шаг: сначала собрать empty/loading surfaces, потом вынести публичный landing отдельно от feed-page
- 2026-03-15 | P1 | area:frontend-ui | Добить `detail drawer` для карточки поста и решить, нужен ли отдельный route для landing
  - Контекст: page-level visual MVP почти закрыт; осталось добавить слой раскрытия поста и при необходимости разделить информационный верх и живую ленту по маршрутам
  - Следующий шаг: спроектировать drawer без слома текущего grid и data-flow `/api/front`
- 2026-03-15 | P2 | area:frontend-ui | После проверки на телефоне решить, нужен ли отдельный mobile-specific polish для drawer и hero
  - Контекст: desktop visual MVP уже собран; возможный следующий слой — адаптация spacing/типографики/scroll поведения на мобильных экранах
  - Следующий шаг: провести мобильный smoke и только после этого принимать решение о дополнительной правке
- [x] 2026-03-15 | P1 | area:import | Прогнан полный переимпорт `data/nu/result.json` + TG-комментариев в локальную БД `5433`
  - Контекст: после импорта подтверждены `tg=1102`, `post_comments=1785`; `3107/3108` больше не идут как посты, а закреплены комментариями к `3106`
  - Следующий шаг: привести канонический локальный API на `:8080` к текущему коду, чтобы фронт всегда смотрел на свежую выдачу без отдельного порта `:18080`

- 2026-03-15 | P1 | area:api-dev | Канонизировать локальный API-процесс на `:8080` под текущий код
  - Контекст: живая проверка после TG-импорта выполнена на `:18080`; старый `:8080` не из текущего кода и может отдавать устаревший JSON
  - Следующий шаг: поднять/переназначить локальный API на `:8080` и перепроверить фронт уже без обходного порта

- [ ] 2026-03-15 | P1 | area:ops | Убрать публичный `:8080` за нормальный reverse proxy/HTTPS для MVP наружу
  - Контекст: для Vercel preview сейчас открыт `91.218.113.247:8080`, этого достаточно для чернового MVP, но не для финального публичного контура.
  - Следующий шаг: поднять Nginx/Caddy на 80/443 и перевести frontend rewrites на HTTPS origin.
- [ ] 2026-03-15 | P2 | area:frontend-ui | Прогнать визуальную проверку Vercel preview и добить mobile polish после первой внешней приемки
  - Контекст: публичный URL уже выдан, но финальная приемка по внешнему виду и мобильному экрану еще не зафиксирована.
  - Следующий шаг: собрать замечания по preview и точечно править spacing/drawer/toolbar.
- [x] 2026-03-16 | P0 | area:data | Локальная compose-БД `mudro11-db-1` безопасно восстановлена через WSL `go run` на канонический DSN `127.0.0.1:5433`
  - Контекст: `docker run golang:*` дважды оказался ложным обходом; фактическое восстановление удалось через прямой WSL-запуск импортеров.
  - Следующий шаг: при следующем восстановлении использовать WSL/host DSN как основной путь, а не временные tool-контейнеры.
- [x] 2026-03-16 | P0 | area:ops | Зафиксировать на VPS стабильный auth-контур Postgres для `mudro-api.service`
  - Контекст: причиной оказался публичный `5433` с суперпользователем `postgres`, плюс серверный `cmd/bot` был загрязнен stale `.go`-файлами вне актуальной репы.
  - Следующий шаг: держать БД только через loopback + firewall guard, а следующий внешний инфраструктурный шаг — убрать публичный `:8080` за reverse proxy/HTTPS.
- [x] 2026-03-16 | P1 | area:data-policy | Зафиксировать VK как snapshot-only источник без регулярного реимпорта
  - Контекст: пользователь подтвердил, что VK больше не нужно поддерживать актуальной выгрузкой; обновлять нужно только TG-контур.
  - Следующий шаг: правило закреплено в `README.md` и `docs/ops-runbook.md`; не планировать cron/update для VK.

- [ ] 2026-03-16 | P1 | area:openclaw | Разобрать и либо восстановить, либо законсервировать legacy OpenClaw-контур на VPS (openclaw-app Exited (1), pn-test-compose.service active (exited), порт 18789 не слушает)
  - Контекст: для текущего MVP Mudro OpenClaw не нужен, но на сервере остался полуразобранный контур, который создает ложный сигнал о «живом» сервисе.
  - Следующий шаг: принять решение — отдельный recovery OpenClaw или полная консервация до следующей фазы.
- [ ] 2026-03-16 | P1 | area:openclaw-security | Закрыть security warning OpenClaw для small-model fallback (ollama/qwen2.5-coder:3b): либо sandbox ll, либо отдельная безопасная политика для local-agent
  - Контекст: runtime уже восстановлен и Telegram жив, но openclaw security audit по-прежнему ругается на small model без sandbox.
  - Следующий шаг: выбрать между полной sandbox-политикой для local/fallback или осознанным исключением с документированным риском.
- [ ] 2026-03-16 | P1 | area:openclaw-models | Если потребуется именно более новая cloud-модель, перевести OpenClaw с openai-codex OAuth-провайдера на официальный OpenAI API provider и выбрать актуальный публичный GPT-5.x-Codex
  - Контекст: текущий runtime OpenClaw 2026.3.2 реально видит openai-codex/gpt-5.3-codex; публичного GPT-5.4 как рабочего model-id для этого провайдера не подтверждено.
  - Следующий шаг: делать только как отдельную controlled migration, чтобы не ломать живой Telegram/OpenClaw-контур.

- [ ] 2026-03-16 | P1 | area:frontend-ui | Собрать внешнюю визуальную рецензию по текущему MVP-экрану и после нее сделать только точечный polish (spacing, copy, mobile).
  - Контекст: локальный preview на http://127.0.0.1:5174 уже идет против живого API :18080 и подтвержден browser smoke без console errors.
  - Следующий шаг: показать пользователю viewport screenshot и собрать замечания по первому экрану, карточкам и drawer.
- [ ] После следующей внешней рецензии решить: финальный mobile polish или выкладка обновленного фронта наружу.

- [ ] 2026-03-16 | P1 | area:import-api | Принять второй CSV с чат-комментариями и довести TG comment sync из нового формата
  - Контекст: посты уже синхронизируются через cmd/tgcsvimport; нужен парный importer для комментариев/реплаев с media и parent links
  - Следующий шаг: получить CSV комментариев, зафиксировать его схему и добавить 	gcommentscsvimport без ломки текущего post_comments контура
- [ ] 2026-03-16 | P2 | area:devex | Заполнить `C:\Users\gorit\.codex\secrets\mudro-github-mcp.local.env` и включить `mudro_github` MCP
  - Контекст: локальный wrapper уже есть, но GitHub MCP сейчас падает на старте с `MUDRO_GITHUB_PAT is not set`, поэтому PR/issues/actions через MCP недоступны.
  - Следующий шаг: создать fine-grained read-only PAT для нужных репозиториев (`Contents`, `Pull requests`, `Issues`, `Actions`), записать его в secret-файл и вернуть `enabled = true` в `C:\Users\gorit\.codex\config.toml`.
- [ ] 2026-03-17 | P1 | area:vps-web | При появлении домена перевести VPS frontend-контур с `http://<server-ip>/` на нормальный HTTPS reverse proxy (`80/443`) и после этого решить судьбу внешнего `:8080`
  - Контекст: self-hosted MVP уже живет на nginx :80 и проксирует локальный API/media; сейчас не хватает только доменного TLS-слоя и финального закрытия голого API-порта наружу.
  - Следующий шаг: завести домен/поддомен, выпустить сертификат (Caddy/Nginx+Let's Encrypt), затем проверить, нужен ли внешний 8080 вообще.
- [ ] 2026-03-17 | P2 | area:frontend-ui | После новой внешней рецензии решить только узкий финальный polish self-hosted UI: mobile density, drawer rhythm и уместность нижнего roadmap-блока
  - Контекст: product-pass уже развернут на `http://91.218.113.247/#feed`; header, toolbar и single-column cards выровнены, данные на странице живые.
  - Следующий шаг: собирать только конкретные визуальные замечания по runtime-экрану и не возвращаться к data/API, если не появится новый факт.
- [ ] 2026-03-17 | P1 | area:release | Решить судьбу нового Vercel preview `frontend-nv1pu0992-goritskimihail-2652s-projects.vercel.app`: оставить как preview или продвинуть этот же UI-pass в production alias
  - Контекст: preview собран с уже исправленными именами комментариев, media CTA и полноформатным image rendering, но production alias пока не обновлялся в этом проходе.
  - Следующий шаг: при явном подтверждении выполнить `vercel deploy --prod` из `frontend/` или оставить основным URL self-hosted VPS.
- [ ] 2026-03-17 | P1 | area:repo-windows | Убрать из истории/ветки `main` Windows-несовместимый путь `cmd/bot/ server.go `, из-за которого чистый `git worktree add ... main` падает с `invalid path`
  - Контекст: новый базовый каталог `D:\mudr\mudro11-main` пришлось создавать как ветку `codex/mainline-base` от валидного `HEAD`, а не прямым checkout `main`.
  - Следующий шаг: найти коммит/файл-артефакт, удалить или переименовать его в Linux-контуре, затем проверить, что `main` снова разворачивается в Windows без ошибок.
- [ ] 2026-03-17 | P2 | area:skaro-ui | Если понадобится постоянный live-dashboard без ручного старта, решить формат фонового запуска `Skaro UI` для каждой папки (например, user-level shortcuts или terminal profiles), не смешивая порты `4700..4703`
  - Контекст: локальные launcher/tasks уже готовы, `Skaro status/config` подтверждены, а короткий `ui --no-browser` уходит в штатный server mode; не хватает только удобного always-on режима по желанию пользователя.
  - Следующий шаг: при явном запросе добавить shortcuts/terminal profiles или system tray launchers для `main/automation/bugs/devops`.
